.env
.vscode/
__pycache__/
donnees/export_cours/
venv/
poppler/

# Secrets Google : fournis en CI par les secrets du dépôt, jamais versionnés.
token.json
token.*.json
credentials.json

# Adresses de camarades : des données personnelles de tiers n'ont rien à faire
# dans un dépôt public. Le format du fichier est décrit dans src/partager.py.
donnees/inscriptions.txt
donnees/demandes_discord.json

# Sorties régénérées à chaque exécution.
donnees/edt*.ics
donnees/.verif_*

# Ancien bac à sable, remplacé par le code à la racine.
new_test/
